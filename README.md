```shell
# to install virual environment
python3 -m venv env

# to activate virtual environment
source env/bin/activate

# to see the dependencies
pip freeze

# to install all the dependencies
pip install -r requirements.txt

# to run the demo
python3 src/a.py token
```

